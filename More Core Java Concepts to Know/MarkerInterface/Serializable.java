package MarkerInterface;

public interface Serializable {

    // An interface that does not contain methods,
    // fields, and constants is known as marker interface.
    // In other words, an empty interface is known as marker interface or tag interface.
    // It delivers the run-time type information about an object.
    // It is the reason that the JVM and compiler have additional information about an object.
    // Marker interface is used as a tag that inform the Java compiler by a message
    // so that it can add some special behavior to the class implementing it.  --

}
